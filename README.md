# WebSocketChat client

This is just an experimental project.

# Usage

To be able to join you have to set up the WebSocketChat server first, 
and change the URL for the `new WebSocket()`, no config for it yet.
